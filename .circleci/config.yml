version: 2
jobs:
  build:
    working_directory: ~/
    docker:
      - image: circleci/node:7
    steps:
      - checkout
      - run: npm install
      - run: npm run build
      - store_artifacts:
          path: ./dist
